OperationsCenter:
  Enabled: false
  HostName: ${host_name}

Master:
  Enabled: true
  OperationsCenterNamespace: ${oc_namespace}

Agents:
  Enabled: true
  SeparateNamespace:
    Enabled: ${agent_namespace_enabled}
    Name: ${agent_namespace_name}
    Create: ${create_agent_namespace}

Hibernation:
  Enabled: true
